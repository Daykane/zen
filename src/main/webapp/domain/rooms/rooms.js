(function(window, angular, _){
    

    function rooms(){
        // Init

        // Private variables

        // Private methods

        // Public API
    }

    angular.module('zen.api.rooms', [
            'zen.services'
        ]).factory('Rooms', rooms);
})(window, window.angular, window._);
