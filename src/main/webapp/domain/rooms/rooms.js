(function(window, angular, _){
    'use strict';

    function rooms(){
        // Init

        // Private variables

        // Private methods

        // Public API
    }

    angular.module('zen.api.rooms', [
            'zen.services'
        ]).factory('Rooms', rooms);
})(window, window.angular, window._);
